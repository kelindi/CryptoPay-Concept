{
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.17.1",
    "mongodb": "^4.2.1",
    "mongoose": "^6.0.14"
  },
  "scripts": {
    "start": "node server.js && xdg-open localhost:5000",
    "setup": "npm install && cd client && npm install && cd ..",
    "build": "cd client && npm run build && cd .. && node server.js"
  }
}
