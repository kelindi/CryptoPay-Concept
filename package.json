{
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "connect-mongo": "^4.6.0",
    "express": "^4.17.1",
    "express-session": "^1.17.2",
    "mongodb": "^4.2.1",
    "mongoose": "^6.0.14",
    "multer": "^1.4.4"
  },
  "scripts": {
    "start": "node server.js",
    "setup": "npm install && cd client && npm install && cd ..",
    "build": "cd client && npm install && npm run build && cd .. && node server.js"
  }
}
